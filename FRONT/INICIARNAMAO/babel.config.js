module.exports = {
  presets: [
    '@babel/preset-env',
    '@babel/preset-react'
  ]
}

/**
 *  Propriedade preset 
 * preset-env para meter as funcionadade que o navegar não entende 
 * preset-react as propriedade do rreact que o naegador não entende 
 */